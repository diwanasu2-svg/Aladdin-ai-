# Aladdin AI — Phase 11 Intelligence Module Dependencies
# Add these to your main requirements.txt

# ── Core ML & Data Science ────────────────────────────────────────────────────
scikit-learn>=1.4.0          # RandomForest, SVD, LabelEncoder
numpy>=1.26.0                # Numerical operations
pandas>=2.2.0                # Data manipulation for habit analysis

# ── NLP & Sentiment Analysis ──────────────────────────────────────────────────
nltk>=3.8.1                  # VADER sentiment analyser, tokeniser
textblob>=0.18.0             # NLP utilities (optional, supplements NLTK)

# ── News & Web ────────────────────────────────────────────────────────────────
aiohttp>=3.9.3               # Async HTTP client (NewsAPI, weather, geo)
feedparser>=6.0.11           # RSS feed parsing for news fallback
# newsapi-python>=0.2.7      # Optional: official NewsAPI client

# ── Google Calendar Integration (optional) ────────────────────────────────────
google-api-python-client>=2.120.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
python-dateutil>=2.9.0       # Robust date/time parsing from API responses

# ── Location & Geo ────────────────────────────────────────────────────────────
geopy>=2.4.1                 # Geocoding, distance calculations (Nominatim)

# ── Database (already in stdlib but listed for clarity) ──────────────────────
# sqlite3                    # Built-in Python — no install needed

# ── Audio Processing (mood analyzer — optional) ───────────────────────────────
# librosa>=0.10.1            # Audio feature extraction (speech rate, energy, pitch)
# sounddevice>=0.4.6         # Real-time microphone access

# ── Deep Learning (optional — for advanced mood/habit models) ─────────────────
# tensorflow>=2.15.0         # Full DL framework
# torch>=2.2.0               # PyTorch alternative

# ── Serialization ─────────────────────────────────────────────────────────────
# pickle is stdlib; no install needed

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0                # Test runner
pytest-asyncio>=0.23.0       # Async test support

# ══════════════════════════════════════════════════════════════════════════════
# Post-install setup required:
#
# 1. Download NLTK data for VADER:
#    python -c "import nltk; nltk.download('vader_lexicon')"
#
# 2. Set environment variables:
#    NEWS_API_KEY       — From https://newsapi.org/register (free tier: 100 req/day)
#    OPENWEATHER_API_KEY — From https://openweathermap.org/api (free tier available)
#    GOOGLE_CALENDAR_CREDENTIALS_JSON — JSON string from Google Cloud Console OAuth2
#
# 3. (Optional) Run initial habit analysis:
#    from backend.intelligence import create_intelligence_engine
#    engine = create_intelligence_engine()
#    engine["habit_predictor"].analyse_patterns("default")
# ══════════════════════════════════════════════════════════════════════════════
