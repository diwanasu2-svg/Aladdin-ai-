# keystore.template.properties
# Copy to keystore.properties (project root) and fill in real values.
# DO NOT commit keystore.properties — it is excluded in .gitignore.

storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD
# Path relative to app/build.gradle.kts
storeFile=../release.jks
